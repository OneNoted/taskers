# Taskers Old

This directory contains the archived pre-greenfield product layers.

It is kept for reference and selective cherry-picks only. It is not part of the
active root workspace, root CI, or the default contributor workflow.

The shared crates at the repo root remain canonical. Code under `taskers-old/`
may still reference them instead of carrying a fully duplicated copy.
